#include <hal/irqController.h>
#include <driver/Driver.h>

IRQController* irq_controller = NULL;

/**
 * Initialize the IRQ controller.
 * This function should be called during system initialization to set up the IRQ controller.
 */

void irq_controller_initialize() {
    
}
